server:
  port: 8088
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/uniqdb?useSSL=false&serverTimezone=UTC&useLegacyDatetimeCode=false
    username: uniq_user
    password: uniq1234

  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    show-sql: true
    hibernate:
      format_sql: true
      ddl-auto: update